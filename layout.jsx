export const metadata = {
  title: "Physics in Motion — CSEC Physics (Forms 3–5)",
  description: "Master CSEC Physics with engaging, exam‑focused lessons. Forms 3–5. Fatima College educator with 17+ years experience.",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
